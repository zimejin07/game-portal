import { CasinoConfig } from "@repo/types";

const config: CasinoConfig = {
    name: "Casino A",
    menuPosition: "right",
};

export default config; // will replace this with the proper config data and updateed type
